# 🚀 快速入门指南

## 5分钟快速体验系统

### 步骤1：安装依赖 (1分钟)

```bash
cd /Users/senguoyun/code/test-copy-senguo
pip install -r requirements.txt
```

### 步骤2：启动后端服务 (终端1)

```bash
chmod +x start.sh
./start.sh
```

**预期输出:**
```
==================================================
水果蔬菜采购管理系统 - 后端服务
==================================================
API 文档:
  POST   /api/purchase/create  - 创建采购单
  GET    /api/purchase/list    - 获取采购单列表
  ...
==================================================
启动服务: http://127.0.0.1:5000
==================================================
 * Running on http://127.0.0.1:5000
```

### 步骤3：启动前端服务 (新终端2)

```bash
cd /Users/senguoyun/code/test-copy-senguo
chmod +x start_frontend.sh
./start_frontend.sh
```

**预期输出:**
```
Serving HTTP on 127.0.0.1 port 8000 (http://127.0.0.1:8000/)
```

### 步骤4：访问应用

在浏览器中打开:
```
http://127.0.0.1:8000/frontend/index.html
```

### 步骤5：测试功能

#### 创建采购单
1. 点击底部 "➕ 新增" 按钮
2. 填写表单：
   - 供应商名称: 测试供应商
   - 产品名称: 苹果
   - 数量: 100
   - 单价: 5.5
   - 分类: 水果
3. 点击 "创建采购单" 按钮
4. 应该看到成功提示

#### 查看采购单列表
1. 点击底部 "📋 单据" 按钮
2. 应该看到刚创建的采购单

---

## 运行测试 (可选)

### 后端单元测试

```bash
cd backend
pip install pytest
pytest test_api.py -v
```

### 前端UI自动化测试

```bash
# 确保后端和前端都在运行

cd frontend
pip install selenium
pytest test_ui.py -v -s
```

---

## 项目结构

```
test-copy-senguo/
├── backend/
│   ├── app.py              # Flask应用
│   └── test_api.py         # 单元测试
├── frontend/
│   ├── index.html          # 前端页面
│   └── test_ui.py          # UI测试
├── requirements.txt        # 依赖
├── start.sh               # 启动脚本
├── start_frontend.sh      # 前端启动脚本
├── README.md              # 完整文档
└── PROJECT_REPORT.md      # 项目报告
```

---

## 核心功能

✅ **创建采购单** - 支持供应商、产品、数量、单价、分类
✅ **查看列表** - 实时显示所有采购单
✅ **表单验证** - 完整的输入验证
✅ **实时计算** - 自动计算合计金额
✅ **错误处理** - 友好的错误提示
✅ **响应式设计** - 支持移动端和桌面端

---

## API示例

### 创建采购单

```bash
curl -X POST http://127.0.0.1:5000/api/purchase/create \
  -H "Content-Type: application/json" \
  -d '{
    "supplier_name": "测试供应商",
    "product_name": "苹果",
    "quantity": 100,
    "unit_price": 5.5,
    "category": "水果"
  }'
```

**响应:**
```json
{
  "code": 200,
  "message": "采购单创建成功",
  "data": {
    "id": "PO1001",
    "supplier_name": "测试供应商",
    "product_name": "苹果",
    "quantity": 100,
    "unit_price": 5.5,
    "total_amount": 550.0,
    "category": "水果",
    "status": "待审批",
    "created_at": "2024-01-12 16:30:00"
  }
}
```

### 获取采购单列表

```bash
curl http://127.0.0.1:5000/api/purchase/list
```

---

## 常见问题

**Q: 后端无法启动？**
A: 检查Python版本 (需要3.8+) 和依赖是否安装

**Q: 前端无法访问？**
A: 确认前端服务在运行，检查URL是否正确

**Q: 如何停止服务？**
A: 在对应终端按 Ctrl+C

**Q: 如何修改端口？**
A: 编辑 `backend/app.py` 最后一行和 `start_frontend.sh`

---

## 下一步

1. 📚 查看 `README.md` 了解完整功能
2. 🧪 运行测试用例验证质量
3. 🎨 修改前端样式自定义外观
4. 🔧 扩展API添加更多功能

---

**祝你使用愉快！** 🎉
